{{#ifEq type 'textarea'}}
<textarea class="{{ class }}" name="{{ name }}" data-validation="{{ validationType }}" placeholder="{{ placeholder }}">{{ value }}</textarea>
{{else}}
<input type="{{ type }}" class="{{ class }}" name="{{ name }}" value="{{ value }}" data-validation="{{ validationType }}" placeholder="{{ placeholder }}">
{{/ifEq}}